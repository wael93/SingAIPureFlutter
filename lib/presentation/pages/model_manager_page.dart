// TODO: Implement model manager page
import 'package:flutter/material.dart';

class ModelManagerPage extends StatelessWidget {
  const ModelManagerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Model Manager'),
      ),
      body: const Center(
        child: Text('Model Manager Page - To be implemented'),
      ),
    );
  }
}
