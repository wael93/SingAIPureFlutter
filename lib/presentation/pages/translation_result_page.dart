// TODO: Implement translation result page
import 'package:flutter/material.dart';

class TranslationResultPage extends StatelessWidget {
  const TranslationResultPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Translation Result'),
      ),
      body: const Center(
        child: Text('Translation Result Page - To be implemented'),
      ),
    );
  }
}
